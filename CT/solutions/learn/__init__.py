"""模仿学习（BC）逼近 MILP 资源服务序：特征提取 / 标签 / 策略网络。

设计见《计划：用模仿学习（纯 BC）逼近 MILP 的资源服务序》。核心：timing.py 已证明是 ms 级精确
评估器，全部 gap 在「资源服务序」；本包学一个按步派工策略（每步从合法候选 hop 选下一个），
推理走 timing.py 的 Banker 安全解码（保证无死锁）+ solve_timing 求时刻。
"""
