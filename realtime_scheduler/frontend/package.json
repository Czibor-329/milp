{
  "name": "ct-realtime-scheduler-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "CT 实时调度终端的 TypeScript 前端构建配置",
  "scripts": {
    "build": "esbuild src/config_editor.ts --bundle --format=esm --target=es2022 --outfile=assets/config_editor.js && esbuild src/route_editor_logic.ts --bundle --format=cjs --platform=node --target=node18 --outfile=route_editor_logic.js && esbuild src/workspace_visualizer.ts --bundle --format=cjs --platform=node --target=node18 --outfile=workspace_visualizer_logic.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
