# Agent Instructions

## 写代码规范

1. 函数必须有文档说明。
   - 公共函数、导出函数、复杂函数必须说明用途、参数、返回值和关键副作用。
   - 简单函数也应至少说明它解决什么问题。

2. 文件必须有文档说明。
   - 每个源码文件顶部应说明该文件的职责、主要内容和与其他模块的关系。
   - 如果文件包含重要约束、输入输出格式或业务假设，也应在文件文档中写明。

3. 命名优先使用清晰、常见的完整单词。
   - 常用概念应尽量使用项目内统一的单词表或已经出现过的标准命名。
   - 局部跨度很短、含义明确的变量可以使用缩写，例如循环下标或临时变量。
   - 作用范围较长、跨函数传递、跨模块暴露或经常使用的名称，应使用单词表中的单词，或者使用可读性高的完整单词。
   - 避免使用只有作者自己能理解的缩写。

4. 较长函数必须写清主要流程注释。
   - 当函数包含多个阶段、分支较多或阅读时需要上下文切换，应在关键流程块前写简洁注释。
   - 注释应说明这一段在整体流程中的作用，而不是逐行重复代码含义。

5. 注释和文档字符串必须使用中文。
   - 源码中的文件文档、函数文档、类文档和流程注释默认使用中文。
   - 必要的英文专有名词、协议字段名、错误码、类名和函数名可以保留原文。

6. 硬编码数字必须命名，避免裸写魔法数字。
   - 来自接口文档、协议、枚举、业务规则或容差的数字，应定义为有语义的常量或枚举后再使用。
   - 只有非常局部且含义不需要解释的数字可以直接出现，例如列表下标或简单计数。

7. 不准写薄的包装层。
   - 如果一个函数、类或模块只是转调另一个实现，没有增加稳定抽象、统一语义、错误处理、资源管理或明显的调用方简化，就不要新增这一层。
   - 入口函数可以存在，但必须代表真实边界；不要为了“看起来分层”而创建只包一行调用的间接层。

8. 更新前端后必须同步更新对应版本号。
   - 修改前端源码、样式、模板或前端构建产物后，必须更新其对应的资源版本号或缓存版本号，确保浏览器不会继续使用旧缓存。
   - 提交前应确认页面引用的版本号与本次前端改动一致。

9. 更新调度功能后必须同步更新使用文档。
   - 修改快速开始、输入接口、约束语义、调度策略、定时层、Machine 动作可行性、LoadLock 管理或结果分析功能时，必须同步更新 `realtime_scheduler/data/documentation/*.md` 中对应页面，并在“使用文档”Tab 验证左侧换页、正文渲染和右侧页内目录。
   - 每个 Markdown 文件代表一个独立页面，必须保留 `title`、`slug`、`group`、`order`、`description` front matter；正文一级标题必须与 `title` 一致。内容过长时应按主题拆页，而不是继续堆叠到单页。
   - 标准接口文档必须保持在文档导航末尾；接口字段、类型、枚举、继承或聚合关系变化时，必须同步更新接口总览、设备对象、作业对象和 Move/输出页面，不能只更新摘要字段。
   - 文档正文属于本地运行数据，必须继续由 `realtime_scheduler/data/.gitignore` 排除，不能为了提交文档变更而解除 Git 忽略；代码仓库只提交文档读取、校验与展示能力。
